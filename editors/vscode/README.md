# Web Architecture Engine for VS Code

This thin client starts the project-local `wae-lsp` binary and exposes WAE diagnostics, architecture
hover information and server-provided `WorkspaceEdit` quick fixes. Install `@don-erfan/wae` in the
workspace or configure `wae.server.path` with an absolute executable path.
