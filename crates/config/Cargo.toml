[package]
name = "wae-config"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
wae-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
