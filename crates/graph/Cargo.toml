[package]
name = "wae-graph"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
wae-core = { path = "../core" }

[dev-dependencies]
criterion.workspace = true

[[bench]]
name = "graph_scaling"
harness = false
